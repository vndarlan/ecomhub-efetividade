# Credenciais EcomHub
# IMPORTANTE: Nunca commite o arquivo .env com credenciais reais!
ECOMHUB_EMAIL=seu_email@example.com
ECOMHUB_PASSWORD=sua_senha_aqui

# Configurações do Servidor
ENVIRONMENT=production
PORT=8002

# Notas:
# - ENVIRONMENT pode ser "local" (Chrome visível) ou "production" (headless)
# - PORT é a porta onde o servidor vai rodar (padrão: 8002)
